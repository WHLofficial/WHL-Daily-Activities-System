// WHL 竞猜系统 — 纯 Worker（与赛事系统同构）
// 部署前先创建 D1 并回填 database_id；secrets：SYNC_SECRET / CRON_SECRET / SETUP_TOKEN / SYNC_BASE_URL
{
  "name": "whl-guess",
  "main": "src/index.ts",
  "compatibility_date": "2025-09-01",
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "not_found_handling": "single-page-application",
    "run_worker_first": ["/api/*"]
  },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "whl-guess",
      "database_id": "TODO_REPLACE_WITH_D1_ID"
    },
    // 赛事系统（whl-tournament）的 D1：共享账号只读其 user 表
    {
      "binding": "TOUR_DB",
      "database_name": "whl",
      "database_id": "ec3cc695-70bc-47ab-a454-5ca62ec22dd6"
    }
  ],
  // 赛事系统的 KV：会话真源（sess:<token> -> {userId}），跨项目绑定同一命名空间
  "kv_namespaces": [
    {
      "binding": "SESSION_KV",
      "id": "87e2d78308bc47e9b36dc6de53be0458"
    }
  ],
  // 每 5 分钟重试未到账发放项；每日 09:00（北京时间）对账昨天
  "triggers": {
    "crons": ["*/5 * * * *", "0 9 * * *"]
  },
  "observability": {
    "enabled": true
  },
  // Smart Placement：让 Worker 计算靠近 D1（亚太）数据中心，削减跨洋查询往返
  "placement": {
    "mode": "smart"
  }
}
