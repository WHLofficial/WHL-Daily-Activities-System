# PRD · WHL 竞猜系统

- 版本：v0.1（收敛初稿，已确认落盘）
- 日期：2026-09-06
- 状态：开发中（MVP 代码已全部落盘）
- 进度（2026-09-08）：MVP 已实现并通过本地端到端冒烟测试（scripts/smoke-test.sh）；待部署与插件对接
- 配套文档：[TECH_DESIGN.md](./TECH_DESIGN.md)

## 一句话定位

群友在网页上**免费**竞猜比赛（猜比分为主，辅以趣味玩法），猜对按**档位**赢积分；发起人在网页开盘、录比分，积分经 AstrBot 自动发放到手，战报自动发回 QQ 群，两边账每日对平。

> 术语：**竞猜** = 一次开盘活动，分「纯猜胜负」与「标准」两种形式；**玩法项** = 可猜的具体题目。**纯猜胜负**只有一道题，覆盖全部场次（2~10 场），每场都要选出胜负，按猜中的场数给分；**标准**每场比赛可设若干个玩法项（比分 / 胜平负 / 自定义趣味题，1~3 场），另可加一道覆盖全部场次的「猜胜负」。「比分」一项同时判比分全中、总进球、胜平负三档，取最高档。

## 推荐方案总览（结论先行）

- **形态**：独立 Web（手机优先），Cloudflare Pages + Workers + D1，与赛事系统同主域共享登录。
- **玩法**：免费提交预测 + 档位奖励，不是下注开盘；门票/赔率类玩法留到 P2。
- **积分**：AstrBot 是积分唯一真源；发奖走「全局唯一单号 + 幂等 API」，每日自动对账，差异一键修。
- **权限**：管理员（你）+ 少数固定发起人，两级角色进 MVP。

## 功能全景清单

### A. 竞猜生命周期（P0 全部，构成最小闭环）

| # | 功能 | 级别 | 说明 |
|---|------|------|------|
| A1 | 创建竞猜 | P0 | 选玩法形式：纯猜胜负（2~10 场，只有一道胜负题）或标准（1~3 场）；标题、比赛（主客队/开赛时间）、标准形式的单场玩法项（比分/胜平负/趣味题）、可选的跨场次「猜胜负」、各玩法奖励档位（默认可改）、奖励上限、提交截止时间 |
| A2 | 状态流转 | P0 | 草稿→开放→截止→已结算→已发奖→归档；截止可手动提前；发奖前可改比分重算 |
| A3 | 提交预测 | P0 | 每个玩法项一份答案，截止前可改，截止后锁定 |
| A4 | 我的预测 + 本次竞猜概览 | P0 | 看自己的答案、参与人数；别人的答案随时可见（只显示昵称，不露 QQ） |
| A5 | 录入结果与自动判定 | P0 | 发起人填实际比分，系统按档位自动判定每份预测对错与应得积分；趣味题由发起人逐题人工标对错 |
| A6 | 结算预览与确认 | P0 | 发奖前列出每人得分明细，发起人确认后才生成发放批次 |
| A7 | 批量发奖 | P0 | 一键发奖，逐条幂等，失败自动重试，状态可见（已发/重试中/失败） |
| A8 | 战报回群 | P0 | 结算确认后自动生成战报（本期结果、中奖名单、得分变动），由 AstrBot 插件拉取发回群 |
| A9 | 对账 | P0 | 每日自动比对竞猜侧「应收」与插件侧「实发」，差异告警；P0 出数据，P1 出对账台界面 |
| A10 | 冲正 | P0/P1 | 流水类型位 + 发奖前重算属 P0；发奖后一键冲正（发一笔反向流水修正并留痕）属 P1 |

### B. 身份与账号

| # | 功能 | 级别 | 说明 |
|---|------|------|------|
| B1 | 复用赛事系统登录 | P0 | 已实现：跨项目共享赛事系统 KV 会话 + D1 用户镜像，同主域部署即可用；发起人=管理员在后台勾选的名单 |
| B2 | QQ 绑定（提交前置） | P0 | 已实现：网页生成一次性绑定码 → 群里向 bot 发码 → 插件回调完成映射；一码一次、10 分钟有效、一 QQ 一账号；**未绑定不能提交预测**（403 引导去绑定页） |
| B3 | 解绑 / 换号 | P1 | 管理员操作 |

### C. 玩法扩展

| # | 功能 | 级别 |
|---|------|------|
| C1 | 只猜胜平负快速盘（模板化开盘） | P1 |
| C2 | 趣味事件题 | P0（人工判定，机制同档位） |
| C3 | 门票类玩法（扣分参与，双向同步） | P2 |
| C4 | 固定赔率 / 奖池分成类玩法 | P2 |

### D. 榜单与社交

| # | 功能 | 级别 |
|---|------|------|
| D1 | 累计得分总榜 | P1 |
| D2 | 个人战绩页（参与数/命中率/高光时刻） | P1 |
| D3 | 预测动态流（截止前看大家答案） | P1 |
| D4 | 截止前 bot 提醒 | P1 |
| D5 | 神棍榜 / 倒霉蛋榜、跟单、竞猜许愿池 | P2 |

### E. 管理与安全

| # | 功能 | 级别 |
|---|------|------|
| E1 | 角色分级（管理员/发起人固定名单） | P0 |
| E2 | 操作日志（谁开盘/谁录分/谁发的奖） | P1 |
| E3 | 数据导出、黑名单 | P2 |

## MVP 范围

全部 P0。验收口径：

**开一期（纯猜胜负 2~10 场，或标准 1~3 场）→ 群友绑定并提交预测 → 截止 → 录比分 → 算奖 → 一键发奖 → 积分在 AstrBot 侧准确到账 → 战报回群 → 当日对账平，全程零人工修账。**

## 明确砍掉（本期不做）

- 群里 bot 收注/下注（bot 只发通知不收注）
- 真钱、支付、提现
- 外部比分数据源接入（比分手动录入）
- 多语言 / 多时区（按北京时间）
- 防多号强风控（绑定码天然一号一号）
- 赔率引擎（免费猜模式用不上）

## 开放问题（不阻塞开工，需拍板规则）

1. 一份比分预测同时命中「比分对 + 胜平负对 + 总进球对」时，奖励取最高档还是各档都发？**假设：取最高档**。
2. 是否要有「全中加成」（1~3 场全对额外加分）？**假设：P1 再议**。
3. 奖励档位具体数值（如胜平负 50 / 总进球 100 / 比分 300）？**假设：做成配置，默认值由管理员定**。

## 脑暴决策记录（2026-09-06 会话）

| 决策点 | 结论 |
|--------|------|
| 玩法模式 | 免费猜 + 档位奖励（非下注开盘，Q3 修正） |
| 参与门槛 | 不扣分，免费猜 |
| 赔率机制 | 作废（改为档位奖励表） |
| 战报 | 自动发回 QQ 群 |
| 奖励档位 | 全局默认档 + 发起人每场可改 + 单场奖励上限 |
| 规模 | ≤30 人，1~2 场并行 |
| 发起人 | 少数固定发起人（两级角色进 MVP） |
| 错账处理 | 系统冲正（反向流水留痕） |
| 登录 | 复用赛事系统账号（同在 Cloudflare），+ 一次性绑定码映射 QQ |
| 服务器 | 国内、仅 IP、远期香港 → Cloudflare Tunnel 方案 |
| AstrBot 插件 | 积分存 SQLite；能否挂 HTTP 未验证 → 按可挂假设，备 sidecar 退路 |
