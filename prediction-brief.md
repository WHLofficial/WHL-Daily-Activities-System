# 竞猜系统 · 脑暴任务书

## 目标
从零设计一个竞猜系统。本轮只出方案，不写代码。最终交付两份东西：
1. PRD：功能全景清单（每项标 P0/P1/P2）+ MVP 范围 + 明确砍掉什么
2. 技术方案：部署架构 + 积分同步机制 + 数据模型草图

## 背景
- 代码主要由你写（vibecoding），我是需求方。术语用人话解释，别甩缩写。
- 形态：独立 Web 系统，用 Cloudflare Pages + Workers 架构开发。
- 玩法：管理员手动开盘口（比分、MVP、任意事件都行），群友用积分下注，开奖派奖。
- 积分体系在另一台云服务器的 AstrBot 积分插件里，群友的积分都在那边。竞猜系统必须和它数据同步。
- 关键前提：那个积分插件是我自己写的，可以给它加同步 API。
- 规模：朋友局量级，但设计不许锁死，以后要能扩。

## 约束
- 先发散后收敛：发散阶段给至少 3 个互不相同的切入方向（例：按玩法机制切 / 按积分同步架构切 / 按社交玩法切），各列差异化功能面；收敛时用「MVP 只留 P0」的口径砍。
- 积分同步是本项目最重的技术风险，方案必须覆盖失败路径，不许只画正常流程：下注瞬间扣分的一致性、超时重试、开奖失败回滚、两边对账防重。
- 联网调研：CF Workers 调外部服务器 API 的同步实践、竞猜/预测类产品的赔率机制，标来源；查不到就标「假设」，不许编。
- 信息不足处标「假设:xxx」，不要替我发明需求。
- 技术选型每项两句话内说完：用什么 + 为什么 + 代价。

## 逐条回答这些问题
1. 积分真源选哪种：竞猜侧记账+定时对账 / AstrBot 为准实时读写 / 混合？给推荐和代价，并顺手给出该给我插件设计的同步 API 长什么样（幂等、鉴权怎么处理）。
2. Workers（CF）调云服务器 API，下注扣分怎么防超扣、防重复扣？超时、重试、对账各怎么做？
3. 竞猜的域模型怎么建：盘口 / 选项 / 注单 / 开奖 / 派奖 / 流水的关系？手动开盘的生命周期（开盘→下注→封盘→结算→派奖）每一步数据怎么变？
4. 赔率机制选哪种：固定赔率还是奖池分成？朋友局场景给推荐，说明被薅羊毛的风险各在哪。
5. 身份怎么打通：AstrBot 那边的用户（QQ 号）和 Web 用户怎么对上？登录和映射方案给 1-2 个。

## 验收标准
- 功能清单覆盖竞猜全生命周期：开盘→下注→封盘→开奖→派奖→对账，每项标 P0/P1/P2。
- P0 合起来是一个真实用得起来的最小闭环：开一盘 → 群友下注 → 开奖 → 积分准确变动 → 两边账对得上。
- 数据模型草图（表名+关键字段+关系）画清同步边界：哪些数据在竞猜库、哪些在 AstrBot 侧、经什么接口传。
- 同步方案有完整失败路径：超时 / 重试 / 回滚 / 对账，不是只画 happy path。
- 结论先行：先给推荐方案，发散过程和对比细节放后面。

## 交互方式（多轮会话）
- 第 1 轮：只发散 + 只提问。先给切入方向，并列出你全部想问的问题（编号清单，标出哪个最关键），先别收敛。
- 提问节奏：一次只问一个，从最要命的开始，每问带一句"为什么问这个"。我可以说「跳过」或「你标假设」，不许卡死在某一题。
- 问完（或我说够了）：收敛成 PRD + 技术方案初稿。
- 我确认后：落成 PRD.md 和 TECH_DESIGN.md 存进工作区。
